commands:
    database:
        command: mkdir -p /var/nifi/repository/database
    content:
        command: mkdir -p /var/nifi/repository/content
    flowfile:
        command: mkdir -p /var/nifi/repository/flowfile
    provenance:
        command: mkdir -p /var/nifi/repository/provenance
